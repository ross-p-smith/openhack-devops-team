pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') { 
            steps {
                echo "Going to checkout" 
                checkout scm
                echo "Build docker here"
                // sh "git rev-parse HEAD > .git/commit-id"
                // def commit_id = readFile('.git/commit-id').trim()
                // println commit_id
            }
        }
    }
}